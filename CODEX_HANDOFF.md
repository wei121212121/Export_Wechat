# 微信聊天记录导出工具｜项目交接说明

## 一、项目主线

本项目的主体是微信聊天记录导出，不是语音模型。

目标是为中文 Windows 用户提供一个本地化的聊天记录处理工具，将用户本人有权访问的数据导出为 TXT、Markdown 等格式，便于阅读、检索、备份和后续分析。

语音转文字模型是附加能力，只在用户需要处理语音消息时使用。它不决定项目名称、核心流程或主要发布内容。

## 二、核心功能边界

核心功能包括：

- 读取或接收微信聊天数据；
- 按会话、时间或消息类型进行整理；
- 导出为 TXT、Markdown 等文本格式；
- 将导出结果保存到本地目录；
- 尽量保持离线处理，减少数据离开本机的机会。

具体可用格式和数据来源取决于实际内测程序版本。不要把当前内测包描述成已经拥有完整、稳定、可维护的原始源码工程。

## 三、当前技术状态

目前没有取得原始 Rust/Tauri 源码。现有材料主要包括：

- 已有 Windows 可执行程序；
- 对内嵌前端资源进行补丁的构建脚本；
- 启动、卸载和模型路径辅助脚本；
- 内测说明与交接材料。

因此，本仓库目前是“发布材料和构建交接仓库”，不是完整的软件源码仓库。后续长期维护的第一优先级，是取得原始源码、依赖清单、正式构建流程和可验证的测试数据。

## 四、可选语音组件

语音模型只对应一个附加流程：

`微信聊天记录 → 识别语音消息 → 生成文字`

它不参与普通文本、图片、链接等聊天记录的基本导出。模型文件较大，使用 Git LFS 管理，并单独标注为可选组件。

模型目录约定：

```
程序目录/
├─ 启动内测版.cmd
├─ 导出结果/
└─ models/
   └─ sensevoice/
      ├─ model.int8.onnx
      └─ tokens.txt
```

## 五、发布和隐私边界

禁止提交到公开仓库的内容：

- 微信聊天记录和导出结果；
- 微信数据库、密钥、账号缓存和授权令牌；
- 联系人姓名、手机号、头像等个人信息；
- 本机绝对路径、调试日志和临时文件；
- 未经检查的完整便携包；
- 与项目无关的用户文件。

公开发布前必须单独检查程序包、脚本、模型包和文档，避免把测试数据或本机信息一起打包。

## 六、测试建议

- 使用测试账号或数据副本；
- 先验证文本聊天导出，再验证语音转文字；
- 不要把真实导出作为自动化测试；
- 不要在测试过程中强制终止微信或导出程序；
- 分别验证导出文件内容、编码、目录位置和模型是否可选加载；
- 未完成源码和完整回归测试前，应称为“内测版”，不要宣称为正式稳定版。